<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.karashok.library.banner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_TASKS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_config"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".banner.demo.BannerActivity" />
        <activity android:name=".banner.demo.demo.BannerAnimationActivity" />
        <activity android:name=".banner.demo.demo.BannerStyleActivity" />
        <activity android:name=".banner.demo.demo.IndicatorPositionActivity" />
        <activity android:name=".banner.demo.demo.CustomBannerActivity" />
        <activity android:name=".banner.demo.demo.BannerLocalActivity" />
        <activity
            android:screenOrientation="portrait"
            android:name=".banner.demo.demo.CustomViewPagerActivity"/>
        <activity
            android:name=".ultraviewpager.demo.UPVDemoActivity"
            android:label="@string/title_activity_main"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Holo.Light"/>
        <activity
            android:name=".ultraviewpager.demo.PagerActivity"
            android:theme="@android:style/Theme.Holo.Light" />
    </application>

</manifest>